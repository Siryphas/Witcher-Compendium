Using the json data:

- before starting development execute "npm run build"
  - This will create the compendia from the tracked json-files
- edit the entries in Foundry
- close Foundry (!important)
- when you are done run "npm extractPacks"
  - commit only the updated json files